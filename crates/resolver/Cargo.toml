[package]
name = "resolver"
version = "0.1.0"
edition = "2021"

[dependencies]
logger = {  path = "../logger" }
semconv = { path = "../semconv" }
schema = { path = "../schema" }
version = { path = "../version" }

thiserror.workspace = true

