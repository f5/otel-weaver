[package]
name = "resolver"
version = "0.1.0"
edition = "2021"

[dependencies]
semconv = { version = "0.1.0", path = "../semconv" }
schema = { version = "0.1.0", path = "../schema" }

thiserror.workspace = true